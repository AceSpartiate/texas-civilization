// The regressions the storming of Béxar's checks guard (scripts/battle-bexar-injections.mjs runs them). Each is one exact piece
// of the code replaced with the mistake; the check written for it, and only it, must fail.
const T = 'tests/battle-bexar.test.mjs', V = 'tests/battle-bexar-view.test.mjs';
export const UNIT = [
  { name: 'the men are set down in the town rather than walking there', file: 'sim/bexar-fight.mjs',
    from: '    const part = gap <= step ? 1 : step / gap;', to: '    const part = 1;',
    test: T, expect: 'a yes to Milam walks out of the mill at three and into the town before contact, stands in a division and fights, and cannot be sent away' },
  { name: 'the white flag waits on the reinforcement again', file: 'sim/directors.mjs',
    from: "  once(world, 'white-flag', () => fightStorming(", to: "  if (world.director.milestones.ugartechea) once(world, 'white-flag', () => fightStorming(",
    test: T, expect: 'the white flag comes whether or not the camp\'s companies were ever asked for' },
  { name: 'a hit falls on any day alike, not on Johnson\'s daily losses', file: 'sim/bexar-fight.mjs',
    from: '  for (const one of days) { if (roll < one.share) { day = one; break; } roll -= one.share; }', to: '  day = days[Math.floor(unit(`${world.seed}:${personId}:storming-day`) * days.length)];',
    test: T, expect: 'a family\'s man who is hit is hit on a day weighted by Johnson\'s losses, at a moment inside the fighting, in a phase that fits' },
  { name: 'a man\'s fall is sent before it happens', file: 'sim/battle-stage.mjs',
    from: '    const fell = Object.fromEntries(members.filter(one => fates[one] && fates[one].minute <= world.minute)', to: '    const fell = Object.fromEntries(members.filter(one => fates[one])',
    test: T, expect: 'killed: he falls at his staged moment where he stands, his family watching sees it then and not before, and is told only when the word comes' },
  { name: 'the town holds the class between episodes whoever is in it', file: 'sim/battle-stage.mjs',
    from: '      const background = state.phase.background && watchedByAFamily(world, state.battle) ? state.phase.background : null;', to: '      const background = state.phase.background || null;',
    test: T, expect: 'between the episodes the town fights on at two hours a tick while a played family has somebody in it, and not otherwise; never faster' },
  { name: 'the family at home is sent the storming', file: 'sim/bexar-fight.mjs',
    from: '  if (!watching) return { battle: null,', to: '  if (false) return { battle: null,',
    test: T, expect: 'the Host watches it live and is spotlit on the town for each episode; the family in it and the family at the mill watch; the family at home is sent nothing' },
  { name: 'the alert comes only once the men are in the streets', file: 'sim/bexar-fight.mjs',
    from: "  { id: 'entry', from: 'out', until: 'pinned-5', who: 'in',", to: "  { id: 'entry', from: 'feint', until: 'pinned-5', who: 'in',",
    test: T, expect: 'the alert comes through the person before each episode, with Watch on the town; the reserve is told what it hears; nobody else is alerted' },
  { name: 'the account does not say why it ended as it did', file: 'sim/bexar-fight.mjs',
    from: "  const why = 'Why it ended so: the volunteers", to: "  const why = 'It ended so: the volunteers",
    test: T, expect: 'afterwards each family with somebody there is told through that person what happened, what they did and why; the men go home, the wounded stay' },
  { name: 'a gun\'s later shots are sent before they are fired', file: 'sim/battle-stage.mjs',
    from: '    if (Array.isArray(fire)) { for (const at of fire) if (phase.from + at <= minute) shots.push(phase.from + at); continue; }', to: '    if (Array.isArray(fire)) { for (const at of fire) shots.push(phase.from + at); continue; }',
    test: T, expect: 'a page is sent nothing of the storming that has not happened yet' },
  { name: 'the mill left a mile out', file: 'sim/battles/bexar-storming.mjs',
    from: 'export const MILL_OFFSET = Object.freeze({ dx: 0, dy: -0.45 });', to: 'export const MILL_OFFSET = Object.freeze({ dx: 0, dy: -1 });',
    test: T, expect: 'the storming is staged on the town the map draws: its houses are the assembly\'s, the mill half a mile north, the Alamo at the east edge' },
  { name: 'the men inside the houses drawn standing in the street', file: 'public/battle-view.js',
    from: "        if (side.cover === 'loophole' && slot.index % 4 !== 0) {", to: "        if (false) {",
    test: V, expect: 'in the houses most men are out of sight: the fire is flashes and smoke at the walls, the roofs are manned, and the guns fire their dated shots once each' },
  { name: 'the family let out of the house is never drawn', file: 'public/battle-view.js',
    from: '        if (side.civilians) {', to: '        if (side.civilians) continue; if (false) {',
    test: V, expect: 'Karnes\'s door: a man at the bar until it gives, then the hole; the family inside walks out unhurt and never fires' },
  { name: 'Milam is not drawn where he fell', file: 'public/battle-view.js',
    from: '      if (fall.name) view.namedFalls.add(fall.name);', to: '',
    test: V, expect: 'Milam falls in the Veramendi yard at half past three, named, and nobody gives him words' },
  { name: 'no white flag', file: 'public/battle-view.js',
    from: '    for (const flag of battle.flags || []) drawWhiteFlag(ctx, flag, camera, figurePx, time, wind);', to: '',
    test: V, expect: 'dawn on the 9th: the guns stop, the bugle is heard, and a white flag comes to the plaza' },
  { name: 'a family\'s man drawn down before his moment', file: 'public/battle-view.js',
    from: '    if (fell && since >= 0) {', to: '    if (fell || view.members.size) {',
    test: V, expect: 'a family\'s man stands in his unit and fires with it, and falls at the moment the server staged, not before' },
];
export const BROWSER = [
  { name: 'the storming drawn as two lines: no group apart from its side', file: 'sim/battle-stage.mjs',
    from: '  if (phase.groups?.length) {', to: '  if (false) {', expect: 'the fight is drawn as two lines' },
  { name: 'the men who said yes stay at the mill', file: 'sim/bexar-fight.mjs',
    from: "      if (answered(world, 'milam', id) === 'yes') goIn(world, battle, state, person, 'milam');", to: '', expect: 'the alert came only once the fighting had begun' },
  { name: 'the family with nobody there is sent the storming', file: 'sim/bexar-fight.mjs',
    from: '  if (!watching) return { battle: null,', to: '  if (false) return { battle: null,', expect: 'the family with nobody there was sent the battle' },
  { name: 'his own wound is never drawn', file: 'public/battle-view.js',
    from: '    if (fell && since >= 0) {', to: '    if (false) {', expect: 'he was not drawn hurt' },
  { name: 'Milam\'s fall is not drawn', file: 'public/battle-view.js',
    from: '      if (fall.name) view.namedFalls.add(fall.name);', to: '', expect: 'Milam was not seen to fall' },
  { name: 'the Host\'s camera is not sent to the town', file: 'sim/bexar-fight.mjs',
    from: "    const held = Boolean(phase.step && phase.contact);", to: '    const held = false;', expect: 'the Host\'s camera was not on the town' },
];
