# Technical foundation

Maturity: **PROTOTYPE**. The repository implements a bounded Gonzales foundation, not the full game or a classroom-ready deployment. [VISION.md](VISION.md) governs architecture; [HANDOFF.md](HANDOFF.md) and [docs/GATES.md](docs/GATES.md) record executed acceptance evidence. This document describes code behavior, not an independent claim that a gate passed.

## Runtime and source map

The application uses Node 22+ built-ins and browser HTML/CSS/canvas. It has no runtime package dependencies, build step, hosted database, or online AI requirement. It does now ship sprite art, served from this repository over the same server; nothing is fetched from anywhere else at runtime. Browser automation is separate development tooling. `npm start` runs [server/main.mjs](server/main.mjs); `npm test` runs the Node test files.

| Module | Responsibility |
| --- | --- |
| [server/app.mjs](server/app.mjs) | One session, HTTP authority, credentials, commands, checkpoint transactions, SSE snapshots, timer, recoverable faults and class lifecycle |
| [server/storage.mjs](server/storage.mjs) | Versioned checkpoint files, exclusive lifetime save lease, validated ownership release, deliberate class archives |
| [server/deployment.mjs](server/deployment.mjs) | Writable class data folder resolution, save path, ranked join candidates |
| [sim/world.mjs](sim/world.mjs) | Seeded core initialization, canonical entities/routes, physical travel, actions, validation and wire projection |
| [sim/geography.mjs](sim/geography.mjs) | Gonzales-area region generation from the checked claims, and shortest-path routing over the road graph |
| [sim/gonzales.mjs](sim/gonzales.mjs) | Scenario factory that adds the Gonzales directors to a core world |
| [sim/directors.mjs](sim/directors.mjs) | Historical milestones, requests, choices, aggregate battle, consequences and public Host presentation |
| [sim/trade.mjs](sim/trade.mjs) | Offers between households: standing together, what may be swapped, and what each side is told |
| [sim/town.mjs](sim/town.mjs) | The people of Gonzales, their rounds, who trades in what, and the rule deciding what one household may see of anybody else |
| [sim/chores.mjs](sim/chores.mjs) | Farm work as data: a chore is a table entry holding an ordered list of steps, and one interpreter runs them. Skills, tool wear and crop state live here |
| [sim/knowledge.mjs](sim/knowledge.mjs) | Objective truth, audience-specific reports, receipt/observation ages, whether a telling is worth writing down, and courier delivery |
| [sim/encounters.mjs](sim/encounters.mjs) | Riders who carry news, who can hear whom, what a first- and a second-hand account may claim |
| [sim/time.mjs](sim/time.mjs), [sim/routines.mjs](sim/routines.mjs) | Deterministic time compression, important-event barriers, routine food and minor recovery |
| [sim/shops.mjs](sim/shops.mjs) | The shops of the towns: which trades stand where, their invented keepers, what each sells and buys, and what a purchase does |
| [sim/furniture.mjs](sim/furniture.mjs) | Furniture: five pieces made at home or bought from the carpenter, one of each, and the small thing each does under a roof |
| [sim/appearance.mjs](sim/appearance.mjs) | How a family looks: a parent's chosen skin, hair, clothes and hat, beard, bonnet or hair; a child's taken after the parents; read by nothing else |
| [sim/overview.mjs](sim/overview.mjs) | The Host's whole class: every person, animal and wagon where they truly are and every family's land as it stands, picked field by field, sent to the Host only |
| [sim/ending.mjs](sim/ending.mjs) | The end of the game: each family's coin, glory and final number with what earned them, and the Host's closing view — sent only once the class has ended |
| [sim/clock.mjs](sim/clock.mjs) | The two clocks: twenty minutes of effort a tick, and the minutes of 1835 the phase gives it |
| [sim/events.mjs](sim/events.mjs) | Persistent event IDs, causal references and visibility metadata |
| [sim/headless.mjs](sim/headless.mjs), [scripts/simulate.mjs](scripts/simulate.mjs) | Deterministic scenario runners, bot strategies, input capture and internal metrics |
| [public/app.js](public/app.js) | Restricted-snapshot rendering, contextual actions, SSE reconnect and browser evidence hooks |
| [public/family-panel.js](public/family-panel.js) | The family panel's rules, importing nothing: row order, the icon and one-sentence summary for every action, which icon glows, and the stand-in portrait and icon drawing (`docs/FAMILY_PANEL.md`) |
| [public/art.js](public/art.js) | Loads the frontier-v1 atlases and draws a named sprite on its ground-contact point. Every call reports whether it drew, so the renderer's own shapes remain the fallback |
| [scripts/build-atlas-manifest.mjs](scripts/build-atlas-manifest.mjs) | Measures sprite rectangles and anchors from the atlas alpha channel and writes `atlas.json`. Also imported by the acceptance test, which re-measures and compares |
| [public/art-catalog.html](public/art-catalog.html), [public/art-catalog.js](public/art-catalog.js) | Developer page cutting every frame from the shipped sheets at the rectangle the renderer uses, with its anchor marked and whether the game actually uses it |

`server/main.mjs` explicitly selects `createGonzalesWorld`, 15 household slots, port 1835, seed `gonzales-1835`, and a 1000 ms timer interval. Development environment variables are `PLAYERS`, `PORT`, `SAVE_PATH`, `SEED`, `TICK_MS` and `TEXAS_DATA_DIR`. A saved world is loaded in preference to creating a new one; changing the seed does not reset an existing class.

A chore is a list of steps — `walk`, `travel`, `work`, `consume`, `produce`, `field`, `wear`, `mend` — and `advanceChores()` runs one step per person per tick after travel resolves. There is **no randomness anywhere in that path**: `FIC-GONZ-008` requires outcomes to resolve inside a visible risk, so tool wear is a fixed cost per use with the remaining uses shown before the work is chosen, and yields are fixed numbers stated on the control that spends them. `tests/chores.test.mjs` asserts the absence of `Math.random` in that file directly.

Which chores a person may be given, and the reason for any that are refused, are computed on the server and delivered in the projection as `work`. The client renders that answer; it never decides for itself what is possible. That is the fog-of-war rule applied to a control rather than to a fact.

**A cotton field used to come in as food.** `harvest-field` added to `resources.food` whatever the crop was, so half the class ate its cotton — not a balance choice, the game not knowing what it had grown. It now yields `cotton`, which nothing eats, and `sell-cotton` carries it to Gonzales at a fixed rate through the carrying rule every other hauling chore already uses. `HIST-GONZ-022`, `FIC-GONZ-019`.

**The arrival clock** (2026-09-12, `docs/SETTLING_IN.md` step 1). `TIMELINE` in `sim/directors.mjs` counts from dawn on September 28 (`ARRIVAL_MINUTES` = 1080 after the historical minutes from midnight on the 29th). Director code never reads `TIMELINE` directly for a comparison: `momentOf(world, key)` gives a moment on this class's clock, and subtracts the arrival for a class saved without `director.arrival`. `historicalDate` starts from dawn on the 28th when the marker is set.

**The two clocks** (2026-09-15, `docs/COLONIES.md` §5.7 and §6g). `sim/clock.mjs`: `TICK_MINUTES` = 20, the effort a tick always holds; `CALENDAR_SCALE` by `director.phase` (`home` 20, `news` 60, `gathering` 240, `campaign` 720 — the last two unused until build steps 5 and 6); `calendarMinutes(world)`, which answers 20 unless the class is on the real land (`map.source`) and answers 20 again while `deciding(world)` — the night of the crossing, where history fixes both ends of an open question. Nothing is stored, so no save version moved. `stepWorld` advances `world.minute` by it and passes it to `advanceRoutine`, which is entirely calendar work (food, spoilage, a wound mending, rest). `progressTravel` multiplies its ticks by `calendarMinutes / TICK_MINUTES`, so miles an hour of 1835 is constant and `HIST-TEX-006`'s dates hold; a courier's first tick in a stretched phase never carries him past halfway, so a short leg still has a tick of road to be seen on. `sim/encounters.mjs` gains `stretch`, `attention(world, minutes)` and `seenComing(world)`: `PATIENCE_MINUTES`, `PASSING_MINUTES`, `SPEAKING_MINUTES` and `SIGHT_MILES` are counts of ticks wearing fictional clothes, and stretch with the calendar rather than shrinking to a third of themselves. `resolveTimeJump` steps one tick's worth of calendar at a time.

**One person on each horse, ox and wagon, drawn sitting on it** (2026-09-16, owner's playtest). `sim/keeping.mjs` (imports only `sim/travel.mjs`): `holderOf(world, beast)` reads who has a beast from where things are — `borrowedBy` names a person who is on the road with it (`beast.travel`, not `arrive`), standing at its place, or marching (`travel.purpose === 'march'`); a beast on its own family's land, or lent to somebody dead, captured or gone on without it, is nobody's. `hasWords` ("Maria has the horse.", "Maria has the ox and wagon."), `modeWith` (the mode whose needs somebody holds), `bringAlong` (a marching beast set down beside its rider before a journey is judged), `leaveBehind` (a journey releases any beast of theirs it does not take), `keepWithRiders` (after `advanceDirectors` each tick: a held beast of a marcher gets a halted `purpose: 'march'` copy of their journey at their point, and the marcher's `travel.mode` becomes the mode they hold; set down where they are when they leave the ranks otherwise). `sim/world.mjs`: `modeAvailability` refuses on `holderOf` or `promisedTo` (a family member at the beast's place whose chore has `mode` needing it and a `travel` step still to come) before "not here"; `beginTravel` calls `bringAlong` before judging and `leaveBehind` before `harness`; `progressTravel` clears `borrowedBy` only when a beast arrives at its household's home (it cleared on every arrival). `sim/army.mjs` `callHome` sends them home `modeWith` their beasts, and on foot if that way is refused. Every path that starts a journey goes through `beginTravel`, so visits, calls, the march, the rumor, chores and neighbours are all covered; the client now also sends `mode` with `turn-out`. No save version: a saved beast with no borrower is free, and one with a borrower is read by the same rule. Client: `public/motion.js` `wagonDriverId` (the wagon's `borrowedBy`, or on the arrival the principal or first grown person travelling by wagon), `seatOf`, `carriedWithRider`, `seatedClip` (the figure's own `idle-<direction>`), `seatLayout(seat, direction, sizes, rider)` (parts, offsets in person heights, draw order, the rider's `shown` share); `entityClip` gives a horse rider `seatedClip` instead of the courier sheet. `public/app.js` `drawSeated` draws the parts, clipping the rider below the waist; `drawEntity` skips a beast `carriedWithRider`; `window.__seatedDrawn` lists what was drawn. `tests/keeping.test.mjs` (5), `tests/riding.test.mjs` (4), `npm run test:riding` ([browser](docs/evidence/riding-browser.json)). Stand-ins requested in `docs/ART_REQUESTS.md`. `ceiling:` hauling logs behind the ox and harvesting with the wagon use them at home without holding them, so somebody may drive off with the ox mid-haul, as before.
**The teacher sees the whole class** (2026-09-16, owner: *"the teacher should be able to see everything and everyone … players are limited by fog of war, but the teacher shouldn't be"*; [measured](docs/evidence/host-view.json), [browser](docs/evidence/host-view-browser.json)). Before, `projectWorld(world, undefined, 'host')` had no household, so `entities` was empty, `land` null and `observedBy(world, undefined)` returned `[]`: the Host was sent public reports, the director's reconstruction and nobody at all (about 2 KB). Now `sim/overview.mjs` `hostOverview(world)` is called only when `role === 'host'`. Its `everyone` goes out as the Host's `others` — for every entity with a location: id, name, kind, `householdId`, principal, sex, age band, species, laden, resident/about, `carrier` with `facingOf`, location, `task`, `condition`, `withArmy`, `chore` as `{ id, doing, quarry?, plot? }`, and `travel` with only `roadWindow` (±`ROAD_WINDOW_MILES` = 3) of the road and its `base` distance. `overview.lands[householdId]` is `{ name, homeSiteId, view: landView, pieces?, grant (holdingOf bounds), lane?, plots (plotProjection, trimmed), field?, logs?, stock?, arriving? }`, and `overview.army` `{ phase, x, y, strength, camp? }`. Everything is picked by name: `traits`, `skills`, `health` numbers, `report`, `chore.ask`, `resources`, purses, glory and participation never go. A student's projection is untouched: 510 student projections over a 30-family real-land class and a 15-family invented one hashed identical before and after. `public/motion.js` subtracts `travel.base` (absent for a student) in `travelDirection` and `ProjectionMotion.position`. `public/app.js`: `hostView`, `landsOf`, `knownLands` feed `drawPlots`, `drawGroundDetail`'s cleared ground, `drawTerrain`'s field skip, every family's line in `drawHolding` (`__holdingsDrawn`), lanes, each homestead's `view`, house-plot pieces, log pile and stock (`__hostLandsDrawn`) and the family's name as its label; the Host's `others` are drawn with their real work and quarry, culled to the view, and marked `observed` on arrival so the person card is read only (`#action-subject` says so; no order control renders, and `/api/command` still refuses every non-Host action). `#host-goto` lists every family's land and every town and replaces Land for the Host; a town is framed to its street (`applyMapView(id, { street: true })`). Zoom limits are the student's. `ceiling:` the whole class is sent on every snapshot — 87 KB mean, 118 KB worst for thirty families on the real land — rather than only what the teacher's camera holds. No save version moved. Unchanged: the live Gonzales battle still reaches the Host only as the delayed reconstruction (`directorProjection`), and the Host's news is still the public reports.

**The family panel** (2026-09-16, `docs/FAMILY_PANEL.md`, [browser](docs/evidence/family-panel-browser.json)). A client change only: no simulation rule, projection field, command or save version moved. `public/family-panel.js` imports nothing and holds the rules — `panelOrder(members, familyPeople, principalId)` sorts father, mother, children by `age` descending, from `/api/family`; `panelActions({ entity, offered: world.work[id], catalogue, principal, homeId, homesteads, atHome, settable, carry })` turns the server's per-person chore list plus the principal-only orders (`travel` to Gonzales or home, a neighbour's homestead, `work`, `rest`) and `stop-chore` into icons carrying the server's `why`, cost, haul and crop words; `activeKey(entity)` is the glow and reads only `entity.chore.id`, `entity.travel.to` and `entity.task`. `renderFamilyPanel` in `public/app.js` keeps a row per person (`panelRows`) and updates it in place — an icon is the same button from tick to tick, so focus and the popup survive, and a name box is never overwritten while focused, saving, or for four seconds after a save while the world catches up (`mayOverwriteName`). Icons carry the same `data-action`/`data-chore`/`data-destination` attributes the card's buttons did and go through the one click dispatcher; refused icons use `aria-disabled` so they stay focusable and their reason can be read. A portrait sets `selectedId` and `watchedId`, the camera's existing follow. Names: every `input[data-rename]` (panel and family book) saves on `change`, `focusout`, Enter or a `RENAME_PAUSE_MS` (1.5 s) pause through the existing `rename` command. The family book's rows are now rebuilt only when their shape changes; they were rebuilt on every snapshot, which replaced the box under a typing student. `window.__familyPanel` lists rows and glowing icons for proofs. `tests/family-panel.test.mjs` (8); `npm run test:family-panel`.

**The water** (2026-09-15, [record](docs/evidence/rivers.json)). `public/curve.js` holds `curveThrough`: a clamped Catmull-Rom through every point a course gives, used by `landscape-art.js` for water and roads. `drawTerrain` in `public/app.js` draws a river at 0.05 miles and a creek at 0.012, converted through `PERSON_MILES` into `figure` - the same yardstick every tree, cabin and ox is drawn in - so water exaggerates with the map instead of against it. `drawGroundDetail` builds `channels` from the terrain water in view and drops any scattered brush or land tree inside one. `sim/colonies-region.mjs`: a run now records `cut: ['start','end']` where the course carries on out of the kept country, and the exported `joinMouths(courses, within = JOIN_MILES)` carries every other short end onto the course it meets - called last in the build, because a join adds a point and the timber jitter would otherwise shift every later draw.

**The gathering and the march** (2026-09-15, `docs/COLONIES.md` §5.5 and §6h). `sim/army.mjs`: `RENDEZVOUS`/`OBJECTIVE`, `ARMY_MILES_PER_DAY` = 10 read off the calendar, `FALL_IN_MILES`, `volunteersOf`, `withTheArmy`, `formArmy` (at the `organised` milestone), `fallIn` (each tick, for latecomers), `marchingTravel` (a halted journey on the army road, so a marching person is between two places rather than nowhere), `standInTheRanks`, `recordPresent`, `marchOut`, `advanceArmy`, `callHome`/`callHomeRefusal`, `armyProjection`, `armyInvalid`. `world.army = { phase, members, x, y, siteId, progress, road, formedMinute, leftMinute }`. `sim/directors.mjs` gains the milestones `gathering-opens`, `goliad`, `organised`, `march`, `march-on` and `advanceGathering`, which a real-land class enters instead of the old `finish`. `sim/calls.mjs`: `callFor(household, gathering)` answers Gonzales's own call once the gathering has opened. The action is `send-for`, in the answering set so any of the family may be named; the client renders it against the person in `#selection-army`.

**The expresses** (2026-09-14, `docs/COLONIES.md` §6e). `sim/expresses.mjs`: `EXPRESS_LEAVES`, `RELAY_MINUTES`, `distantHouseholds`, `expressRoutes` (the stop tree over towns and crossings from Gonzales), `startExpress` (called from `advanceDirectors` when a word's letter time comes), `advanceExpresses` (each tick after `advanceRelays`: arrival, the wait, `sendOnward` and `relayReport` to the settlement's families), `expressesInvalid`. `world.expresses[topicId] = { leftMinute, routes, heard: { siteId: minute }, sent }`; express riders are couriers with `express: { topicId, from, to, provenance }` and ride like report riders (`beginTravel`'s `riding`). `relayReport` in `sim/world.mjs` wraps `sendRider` with an origin and provenance. `sim/directors.mjs`: a household whose `settlementId` is not Gonzales gets no direct dispatch and no food call or rumor, and `finish` waits up to `EXPRESS_GRACE_MINUTES` for the furthest family to hear the outcome.

**Clearing and the field** (2026-09-14, `docs/LAND_GRANTS.md` §5.1). `sim/fields.mjs`: `PLOT_SIDE`, `CLEARING_SPELLS`, `groundAt`, `fieldPlots(household)` (the plots as counted, no map needed), `plotsOf(world, household)` (stored, or the old `field.cleared` patches read as plots in the corner of the field block), `keepPlots` (writes them down before any change — staking, clearing, fencing, ruin), `clearedPlots`, `sownPlots`, `plotAt`, `OLD_PATCHES`. `sim/improvements.mjs`: `clearedOf`, `isFenced`, `harvestShare` and `standingCrop`/`needsWagonToHarvest` from plots; `clearSpell`, `raiseFence(…, plotId)`, `ruin` to staked; `improvementProjection` now `{ cabin, cleared, fenced, harvestShare, needsWagon }` (no `fence`, no `clearingMax`). Chores `clear-plot` (`plotWork`; steps `stroll: 'plot'`, `clearWork` + `work: SPELL_TICKS`, `clearSpell`, `wear: 'plot'`, `stroll: 'yard'`) and `fence-plot`; `plant-field`/`harvest-field` walk `stroll: 'fields'` (`moreFields`, `chore.visited`), `needsPerPlot`/`consumePerPlot`, and the `field` step sets or clears `plot.sown`. Actions `clear-plot`/`fence-plot { entityId, x, y }` (`plotAt`, then `plotWorkRefusal` in `beginChore` before availability); `advanceChores` abandons a chore whose id no longer exists. `plotsInvalid` checks `work`, `fence`, `sown`. `plotProjection(world, household)` always sends plots, with `spells` on staked ones. `GET /api/plot` takes `job`. Client: `fieldPatch`, `drawPlots` (fields per plot, `window.__plotsDrawn` with state/fence/sown/work), ground scatter skipped in cleared plots, the survey panel generalised (`plotJob`, `#survey-eyebrow`). `scripts/farm-browser-proof.mjs` rewritten for plots, run and passing. Client fixes found by it: `ensureFamily` refetches when the household's members change (a family rolled at Start); `applyMapView` stops watching before zooming or going somewhere.

**Survey** (2026-09-14, `docs/LAND_GRANTS.md` §4.1). `sim/survey.mjs`: `plotRefusal(world, household, point, { ignoring })`, `plotFacts`, `groundAt`, `whereFromHouse`, `stroll`/`strollTarget` (moving a person about their own land a tick at a time with `siteId` kept), `stakePlot`, `plotProjection`, `plotsInvalid`. Chore `survey-plot` (`survey: true`; steps `stroll: 'plot'`, `work`, `stake`, `stroll: 'yard'`), sent only by the `survey-plot { entityId, x, y }` action, which refuses first; `beginChore` takes `extra.plot` into `entity.chore.plot`; survey is left off the work list in the lobby. `household.plots`; `land.plots`. Server `GET /api/plot?x&y`. Client: `survey-start` buttons, `#survey-choose`, `lookAtPlot`, `drawPlots` (`window.__plotsDrawn`). The fresh per-tick payload ceiling in `tests/chores.test.mjs` went from 7000 to 7500 bytes for the one more entry on every person's work list.

**The woods** (2026-09-15, `docs/WOODS_AND_BUILDING.md` §4, `HIST-TEX-016`, `FIC-GONZ-032`). `scripts/build-woods.mjs <bps.tif> <bps.vat.dbf> <tx_eco_l4-dir>` files LANDFIRE's Biophysical Settings into game stands on exactly the elevation grid (`public/terrain/colonies-woods.bin.gz`, one byte an eighth-mile cell) with EPA Level IV ecoregions on a one-mile grid (`colonies-woods.json.gz`); `scripts/terrain/geotiff.mjs` `openClasses` reads the class raster. `sim/woods.mjs`: `STANDS` (succession classes with share, trees an acre and size; kinds; game), `KINDS` (logs by size, use, felling effort, picture), `gridStandAt`, `ecoregionAt`, `standAt(point, { rule, nearCreek })`, `patchAt` (a sixteenth-mile patch's class by hash), `patchCover`, `treeInCell`/`treeById`/`treesIn(box)` (one tree at most a 1/256-mile lattice cell, id `t:<column>:<row>`, nothing stored), `timberMilesFrom`, and `woodsRule(world)`: `landfire` when `world.map.woods === 'landfire-2016'` (set by `buildColoniesRegion` and `createWorld`), `rivers` for a real-land class saved before, `invented` otherwise. `landAround().coverAt(point, rule)` reads the patch under `landfire`; `groundAlong(points, land, rule)`, `layLane(from, to, rule)` and `siteFacts(point, bounds, rule)` pass it; `sim/fields.mjs` `groundAt`, `sim/ways.mjs` `groundAcross` and `sim/homesite.mjs` read `woodsRule(world)`. The land is the same for every class, like the heights. **Drawn** (step 2): `sim/woods-view.mjs` `woodsTile(world, level, tx, ty)` (`shade` 8 miles of one-mile timber shares, `patches` a mile of patch covers, `trees` a quarter mile of `[x, y, kind, size]`) and `woodsCatalogue`, served by `GET /api/woods` and `/api/chores` `woods`; `public/woods-view.js` `ensureWoods` fetches what the view needs once a tile, `drawWoodsCover` paints patches or shade, `timberAt` and `treesVisible` feed `drawGroundDetail`, which draws every tree close up (view of 0.4 square miles or less) at `TREE_SIZES`. **Hunting the family's land** (step 3): `sim/hunting.mjs` `huntingPlace`, `huntFacts`, `huntRefusal`, `stillTicks`, `placeWord`; `CHORES['hunt-land']` shares `hunt-timber`'s steps, is begun by `applyAction` `hunt-land` with `x, y` on foot, and keeps `chore.ground = { x, y, game, cover, toward }` (validated); `advanceChore` walks its `travel` steps with `stroll`, `stalkPoint` and `quarryPoint` read `chore.ground`, and the still wait is `stillTicks(game)`. `/api/plot?job=hunt-land` answers `huntFacts`. `choresFor` sends it no `haul` and drops a `why` it shares with `hunt-timber`. `sim/neighbours.mjs` `huntPlaces` picks the ground on a `landfire` class. **Felling** (step 4): `sim/felling.mjs` `standingTrees`, `fellRefusal`, `fellFacts`, `nextTree`, `fellTicks`, `fellTree`, `recordFelling`, `logsLying`, `oxFree`, `takeUpLogs`, `stackLogs`, `logsProjection`, `fellingInvalid`; `world.woods = { felled: { [treeId]: { by, minute, kind, use, logs, left } }, revision }` and `household.logs = { wall, sill, poor }`, both absent until used. `CHORES['fell-trees']` (`fells`, `applyAction` `fell-trees` with `x, y`, `chore.ground`, `chore.felling`, steps `stroll: 'ground'`, `fell`, `saidFelling`) and `CHORES['haul-logs']` (`hauling`, `haul` with `chore.load`). The trees tile has `stumps: [[x, y, kind, left]]`; `/api/state` has `woodsRevision`; `/api/plot?job=fell-trees` answers `fellFacts`.

**Settlement calls** (2026-09-14, `docs/COLONIES.md` §6f, `FIC-GONZ-031`). `sim/calls.mjs`: `SETTLEMENT_CALLS` (`gather`, `text`, `there`), `callFor`, `offerCalls` (from `advanceDirectors` after `offerRequests`, when a far household holds `cannon-request`), `callAvailability`, `callOptions`, `handleCall` (actions `turn-out` / `stay-put`, routed in `applyAction`), `settleCalls`, `expireCalls` (in `finish`), `callsInvalid`. `world.calls[householdId] = { id, text, status, settlementId, gather, offeredMinute, actorId?, choiceId?, arrivedMinute? }`. `directorProjection` shows a household's call as `request.kind: 'call'` with `answerers`. `sim/neighbours.mjs` answers it, and leaves anybody with task `help` where they are. `sim/ground.mjs` `landAround` is one shared index built per sixteen-mile block (`BLOCK`), its `box` argument now unused; `groundAlong(points, land)` takes the index when a caller has it.

**A family's hunting ground** (2026-09-14, `FIC-GONZ-030`). `sim/chores.mjs` `huntingGround(world, household)` walks `HUNT_BEARINGS` bearings out from the house `HUNT_STEP` miles at a time to `OVERLAND_REACH`, reading `groundAt`, and keeps the nearest cover as site `hunt-<householdId>` `{ kind: 'woods', hunting: true, ownerHouseholdId, cover, toward, fromX, fromY, name }` at its open edge; found again (and `map.revision` bumped) only when the house has moved. `timberFor` sends a hunt there, or to the nearest named stand when it returns null; `stalkPoint` goes in along `toward`; `{cover}` in a step's words and the shot's line says timber or brush. `GET /api/map/homes` serves hunting sites with the homesteads. `sim/ways.mjs` `WAGON_SHORT`: a wagon stretch up to a quarter mile is not refused for its cover.

**Across country** (2026-09-14, `FIC-GONZ-029`). `sim/ways.mjs`: `findWay(world, from, to, mode)` is Dijkstra over the roads costed by their going for that way of going (`segmentPace` on `cutGround`), plus across-country edges out from the start and in to the destination for every site within `OVERLAND_REACH`; `overland` costs a straight line from `groundAcross` (real land: `groundAlong` and `crossings().barrier`; invented map: timber within 1.15 miles of its water, creeks counted, a river is no way) times `OFF_ROAD[mode]`, and refuses the wagon past `WAGON_COVER`. Returns `findPath`'s shape plus `pace` (off-road stretches carry their factor) and `overland`. `beginTravel`, `harness`, `travelRefusal` and `travelModesFor` use it for everybody but a rider carrying word, who keeps to `findPath`; `beginTravel` shifts `pace` by one when the traveller stood apart from the place's point. Expresses, in-person relays, the arrival and glory's miles still use `findPath`. `cutGround` is exported from `sim/geography.mjs`.

**The going and the house site** (2026-09-14, `docs/COLONIES.md` §6d, `docs/LAND_GRANTS.md` §8.4). Only worlds with `map.source === 'texas-colonies-map'` (`onRealLand`). `sim/ground.mjs`: `landAround(box)` indexes the real watercourses near a place (cached per four-mile block) with `nearestWater`, `crossings`, `grade`, `coverAt`; `groundAlong(points)` gives each stretch `[rise m, timber share, brush share, creeks, lesser rivers]`, stored as `route.ground` on every track, lane and bank route (roads carry none); `segmentPace(length, ground, mode)` and `paceOf(points, ground, mode)` turn it into `travel.pace` (`[segment, factor]` runs); `layLane(from, to)` is a wagon's-going A* over an eighth-of-a-mile grid that never crosses a barrier river; `siteFacts(point, bounds)` and `siteWords`. `findPath` returns `ground` per segment (reversed with the rise negated). `sim/travel.mjs` `moveOnGround` and `groundLeft` advance and measure a journey by its going; with no `pace` they are the old arithmetic exactly. `beginTravel`, `harness`, `putOnTheRoad`, relays' overflow and `resolveTimeJump` use them. `sim/homesite.mjs`: `beginArrivals` sets `household.choosingSite`; `chooseSite` (action `choose-site {x, y}`) moves `home-N`, relays the lane from its junction, moves `field-N`, stores `household.mark` and `household.site`, bumps `map.revision`, and puts the family (purpose `arrive`) or anybody homeward on the way to the new site; `holdingOf` keeps a labor round `household.mark`. `waterBurden` multiplies heavy work at home in `advanceChores`; `dig-well` (`wellTicks`, `digWell`, `household.well`). Server: snapshot `mapRevision`; `GET /api/map/homes` (homesteads, their lanes and fields, about 3 KB) which the client patches into its cached map; `GET /api/site?x&y` (`siteFactsFor`, the family's own holding only). Lane cutting: `chooseSite` sets `route.cut = 0`; `laneState`, `laneRefusal`, `cutLaneSpell` (`LANE_TICKS_PER_MILE`), `lanePoint`; chore `cut-lane` (steps `walk: 'lane'`, `work`, `cutLane`, `walk: 'yard'`; everybody on it leaves off together); `findPath` takes the cut stretches' timber and brush out of `ground` (`cutGround`); `land.lane` `{ miles, cut }` in the projection; the client draws the uncut stretch of the family's own lane as stakes (`splitAlong`, `window.__laneDrawn`). Scale: `PERSON_MILES` 0.019 and `CLOSEST_FIGURE` 90 in `public/app.js`, ground detail cells in doublings from 0.01 miles, the yard offsets in `createWorld`/`yardPoint`/`STALK`, the field a quarter mile square. Client: `#site-choose`, a tap on the map while choosing is a place (`lookAtSite`), a stake drawn last (`window.__sitePick`), the tutorial waits while choosing, the supplies line says `water carried N mi` or `well`.

**Automatic neighbours** (2026-09-14, `docs/COLONIES.md` step 3). `sim/neighbours.mjs`: `advanceNeighbours(world, { project, apply })` runs last in `stepWorld` when `world.neighbours` and the class is running; `automatic(world, household)` is `world.neighbours && !household.played`; each family's turn (`thinkFor`) projects `projectWorld(world, id, 'student', { includeMap: false })` and calls `applyAction` for it, swallowing refusals. `judgeOffer(view, offer)` returns `{ take, why }` (`TRADE_VALUE`, `FOOD_KEPT_PER_PERSON`); `ONE_AT_A_TIME` lists one-person errands. `createWorld(..., { neighbours: true })` sets `world.neighbours`; `server/main.mjs` passes it; `server/app.mjs` calls `markPlayed` on join; `validateWorld` refuses a non-true `played`. `respondToOffer` takes a `reason` (trimmed to 120 characters) and writes it into both families' decline records.

**Families across the colonies** (2026-09-14, `docs/COLONIES.md` step 2). `dealCounts(n)` and `START_WEIGHTS`/`SEATED` in `sim/colonies-map.mjs`. `buildColoniesRegion` shuffles seats with the world's random stream, places each family by rejection sampling in a 2–12 mile ring (`LAND_FROM_TOWN`, `WATER_WITHIN`, `STEEPEST`, `ROAD_TO_TOWN`, a one-mile segment index over the built watercourses), splits the nearest reachable road for each track, keeps rivers and creeks of five miles or more within 13 miles of every settlement with families, and puts `settlementId` on each homestead site; `createWorld` copies it to the household and `validateWorld` requires a town. `townOf(household)` in `sim/chores.mjs` resolves the `travel: 'town'` step and `{town}` in a step's words. `sim/town.mjs` `STOREKEEPERS` creates `town-store-<settlement>` entities with `townSiteId`, `deals` and a purse; `advanceTown` walks them at their counters; `recordTrade` names the trader's site. The build adds `<start>-upper-timber`/`-lower-timber` woods sites with tracks, moves a stand to its route's end, and never simplifies a road across a barrier (`clearLine`).

**Colonies map** (2026-09-14, `docs/COLONIES.md` step 1). `scripts/build-colonies-map.mjs` (no arguments; reads `public/terrain`) writes `public/terrain/colonies-map.json.gz`: `places` (id, name, kind town/crossing/ford/camp/confluence/woods, x, y, claimId, start), `crossings` (`<place>:<river>` → point), `roads` (id, from, to, name, kind road/crossing/bank/track, points, miles) and `watercourses` (name, kind river/creek, points; reaches joined). Routing is A* on the terrain grid with a water mask: BLOCKED barrier rivers (gaps at dams joined, ends carried to the map edge), SEA under 0.3 m, SLOW named lesser rivers and creeks; crossings open barrier cells within 0.6 mile. `sim/colonies-map.mjs` loads it (`coloniesMap`, `BARRIER_RIVERS`, `startsOf`). `sim/colonies-region.mjs` `buildColoniesRegion(random, n)` returns the region shape `createWorld` expects; `createWorld`/`createGonzalesWorld` take `{ map: 'gonzales' | 'colonies' }`, `runScenario` takes `map`, and `server/main.mjs` reads `MAP`. `scatterHomesteads` in `sim/geography.mjs` is exported and takes an `accept(candidate)` filter. `public/app.js` draws rivers and creeks near true width when `map.source` is set.

**Real terrain data** (2026-09-14, `docs/LAND_GRANTS.md` §8 step 1). `scripts/build-terrain.mjs <dem-dir> <nhd-dir>` reads USGS 3DEP GeoTIFFs (`scripts/terrain/geotiff.mjs`: classic TIFF, float32, tiled, LZW with the floating-point predictor) and NHD flowline shapefiles (`scripts/terrain/shapefile.mjs`), and writes `public/terrain/colonies-elevation.bin.gz` (Uint16 decimetres, 2424 by 2208 at 0.125 mile, north-west first, 65535 no land) and `colonies-water.json.gz` (header with area, origin, equirectangular projection and grid; watercourses as [name index, flow letter, x, y ... in hundredths of a mile]). `sim/terrain-data.mjs` `realTerrain()` gunzips both once per process and gives `heightAt(x, y)` (bilinear, NaN off land) and `courses`; `milesFrom(terrain, lon, lat)` projects. Not yet used by the world.

**Land grants** (2026-09-13, `docs/LAND_GRANTS.md` step 1). `sim/grants.mjs`: `layOutGrants(sites)` lays out every homestead's grant at league-and-labor size in household order, taking the candidate rectangle nearest centred on the house that overlaps no earlier grant and comes within `HOUSE_CLEARANCE` of no other house, falling back to the largest centred square (`ceiling:`). `createWorld` stores each as `household.grant` (bounds only); `grantOf` works it out from the map for a class saved before. `holdingOf` is the whole grant for a household with `stock: true`, otherwise a labor square round the house clamped inside it. `bring-stock` is a lobby action (`setStock`, `stockRefusal`); `STOCK_SPACE` and `wagonSpaceFor` live in `sim/wagon.mjs`, which uses them for every room check and sends `space` on the lobby wagon projection. `grantProjection` puts `grant` (kind, acres, bounds) on the family's `land`, and `stockChoice` only in the lobby. `advanceArrivals` adds the stock and holding sentences. `validateWorld` refuses a grant that does not hold its own house and a stock mark that is not `true`. Client: `#wagon-stock` radios, `drawHolding` (dashed boundary, `window.__holdingRect`), the book's `data-grant` line, and a herd stand-in.

**The house-raising** (2026-09-13, step 5). `help-raise` in `sim/chores.mjs` has `where: 'neighbour'` and `helps: true`, and no walk: `helpRefusal` (sim/houses.mjs) wants the person on another family's homestead (`hostOf`: same `siteId`, not travelling — the trading rule), the class running, and the host's house between `RAISING_FROM` 0.4 and `RAISING_TO` 0.8 of its work. `beginChore` stores `hostHouseholdId` and `spells` on the chore and records `recordHelpBegun` in both households; the `build` step for a helper calls `buildSpell` on the host and stops at the end of the walls; `finishHelping` (and `stop-chore` in `sim/world.mjs`) records `recordHelpDone`, including the too-late case. `choresFor` offers `help-raise` only to somebody standing on a neighbour's land. `validateWorld` refuses a helper chore naming a household that does not exist or is their own. The client adds `#visit-row` (a select of other homesteads by straight-line distance, and a travel button) to the principal's actions.

**Houses** (2026-09-13, `docs/SETTLING_IN.md` step 4). `sim/houses.mjs` owns `HOUSES` (per layout: `needs` tools, `work` in spells, `room`, `restShare`, `spoilagePerDay`, and the words), `SPELL_TICKS` = 3 and `CROWDED_SHARE`. `household.house` is `{layout, work}`; `plan-house` sets it (household-level, before the lobby gate, no event) until work begins. The `build-house` chore in `sim/chores.mjs` is `walk` then a `houseWork` step (sets `doing` from `stageOf`) with `work: SPELL_TICKS`, then `build`, which calls `buildSpell` and loops back until the house is done; on the last spell every family member on a house chore is finished at once. `buildSpell` sets `improvements.cabin` to sound and records the finish. `shelterOf(world, household)` is what `sim/routines.mjs` now reads for rest at home and food spoiling: the pre-step-4 cabin (1, 0), a finished house (its numbers, crowded if sleepers exceed `room`), or the camp. `choresFor` omits house work for a `houseSettled` family (the payload budget caught the refusal). `noteLandSeen` runs after arrivals each tick and records, on the viewing household, `seenLand[siteId] = {shelter, layout?, minute}` for any homestead one of its people stands on; the renderer draws a neighbour's land from it, or as a camp in an `arrivalClass`. The land projection gains `house`, `home` and (while choosing) `choices`; `/api/chores` carries `houses` with `hours`. `validateWorld` calls `houseInvalid`.

**The wagon load** (2026-09-13, `docs/SETTLING_IN.md` step 3). `sim/wagon.mjs` owns `WAGON_SPACE`, `WAGON_ITEMS` (`kind` stores, tool or good; `space`; `most`; `grants`; `describe`) and `STARTING_POWDER`, which `sim/world.mjs` re-exports. `createWorld` packs `defaultLoad(random)` and spreads `householdFromLoad` into each household: `resources`, `tools` (one key per loaded tool, wear 0; the hoe is a tool like the rest, and a household with a load may have none — `choreAvailability` refuses field work and mending without one, `validateWorld` requires one only of a household with no load), `belongings` and `load` (`[{id, amount}]` in catalogue order). `applyAction` handles `{action: 'load-wagon', item, amount}` before the lobby gate, as it does `roll-family`; `setLoad` refuses through `loadRefusal` and otherwise recomputes stores, tools and belongings from the whole load, writing no event. The catalogue rides once on `/api/chores` as `wagon`; the tick carries `wagon: {used, can, why?}` in the lobby only, because `household` already carries the load. `beginArrivals` marks a packed wagon `laden`, which `progressTravel` clears at the land, and `advanceArrivals` appends `loadSentence`. `validateWorld` checks every tool's wear and calls `loadInvalid`. A household with no `load` is a class saved before this: it keeps its stores, gets no `wagon` projection and is refused repacking. No save version moved.

**Arrival and the camp** (2026-09-13, `docs/SETTLING_IN.md` step 2). `createGonzalesWorld` calls `beginArrivals` in `sim/settling.mjs` for a class with `director.arrival`: every household's `improvements.cabin` becomes `'none'`, every member and piece of property gets a journey with `purpose: 'arrive'`, `silent`, at `WAGON_SPEED` from the fork where the household's track leaves the road, and each journey carries `settle` — the yard spot and task to end on, which `progressTravel` honours. `household.arriving` is set until `advanceArrivals` (after travel, each tick) finds nothing still on the road in and records one `arrival` event. `rollFamily` puts new people on the road beside the wagon. `housed(household)` is what `sim/routines.mjs` reads: rest on the family's own land mends at `CAMP_REST_SHARE` and food is reduced by `CAMP_SPOILAGE_PER_DAY` without a roof. The projection's `land` gains `shelter`, `arriving` and `camp`, and the tick gains `arrivalClass`, which draws every homestead as a camp (a `ceiling:` step 4 replaces). A class saved before has no marker and no `cabin`, which `improvementsOf` reads as standing. Tests of work at home use `tests/support/settled.mjs`.

**Glory, sealed** (2026-09-12). `awardGlory` in `sim/glory.mjs` is the only writer of `world.glory[householdId] = { total, awards }`; `settleHelp` calls it from the participation record. Awards are events with `visibility: 'sealed'`, which `projectWorld` drops before anything else, and `validateWorld` checks each total is the sum of its awards. Nothing in `sim/` or `server/` reads glory — `tests/glory.test.mjs` proves it behaviourally by playing the same class with and without a planted glory and comparing the worlds.

**Concepción** (2026-09-16, `docs/COLONIES.md` §6i). `sim/army.mjs`: `ARMY_MILES_PER_DAY` 14; `campaignRoad` (the road to the Salado, then Espada and Concepción from `MISSIONS` offsets off Béxar; `road.campaign`, `road.stops`), applied at `formArmy` and lazily in `advanceArmy`; `advanceArmy(world, { hold })` clamps progress at `stops[hold]` and sets `army.camp`; `openDetachment`/`askDetachment`/`answerDetachment`/`detachmentRefusal`/`closeDetachment` on `army.detachment.asks`; `fightConcepcion` (participation `concepcion`, `frailty`, `CONCEPCION_DEATH_RISK` 0.01/`_WOUND_RISK` 0.02 through `rollFates`, no limit on deaths). `sim/directors.mjs`: milestones `leave-cibolo`, `detachment`, `to-espada`, `concepcion`, `siege` (the new end; `march-on` no longer ends a class). `sim/world.mjs`: actions `detachment-go`/`detachment-stay` in the answering set. `sim/glory.mjs`: `awardGlory` takes `adjust` (never adds) and `note`. `sim/ending.mjs`: `concepcion` event name, award notes, glory floored at 0 in `finalNumber`. Client: the question in `renderArmyControl` and the sr-only `#army-ours`; `army.camp` names the camp.

**The siege and the Grass Fight** (2026-09-16, `docs/COLONIES.md` §6k). `sim/army.mjs`: `SIEGE_CAMPS` and `moveCamp` (a short `road` with `camp`/`campKey`, named by `campOf`); `ARMY_QUESTIONS` (`storm`, `pledge`, `grass`) with `openQuestion`/`askQuestion` (late arrivals asked in `fallIn`), `questionRefusal`, `answerQuestion` (action `army-answer` with `question` and `answer` in `sim/world.mjs`), `closeQuestion` (open → `silent`), `questionOpen`; `leaveArmy`; `goForClothing`/`returnFromClothing` (`army.furloughs`); `countermandStorm` (glory role `willing`, weight 2); `fightGrass` (`GRASS_WOUND_RISK` 0.03 × frailty, `GRASS_RUN_RISK` 0.01, participation `grass-fight` roles fought/ran/present, `army.grass.outcomes`) and `tellGrassFight`. `armyProjection` `ours[].questions` carries the server's own words for an open question. `sim/directors.mjs`: TIMELINE adds `clothing` … `milam`; `advanceSiege` (each close is `max(date, opened + 6 h)`; `director.phase` is `news` while `questionOpen`); `sendWord` puts news into every household's and the public reports (`learn`), used for Goliad, Concepción, the silver (rumor → contradicted) and the Grass Fight (rumor → confirmed). `public/app.js` `renderArmyControl` draws the questions; the click handler sends `question`/`answer`. Tests `tests/siege.test.mjs`; proof `scripts/siege-browser-proof.mjs`.

**The storming of Béxar** (2026-09-16, `docs/COLONIES.md` §6l). `sim/army.mjs`: `ARMY_QUESTIONS` gains `winter`, `milam` and `reinforce` (a spec's `who` limits who is asked) and is frozen after; `stormedIn`; `STORMING_DEATH_RISK` 0.02, `STORMING_WOUND_RISK` 0.08; `WOUND_GRADES` (`slight` `minor-injury` 3 days; `severe`/`dangerous` condition `wounded` with `health.grade`, 21/60 days; `mark`, `laterDeath`); `fightStorming` (participation `bexar-storming`, `army.storming` outcomes/later/killed), `layDead`, `dieOfWounds` (every later death rolled comes, no limit), `disbandArmy`, `tellStorming`. `sim/world.mjs`: `beginTravel` refuses somebody `wounded`; the student projection carries `marks`. `sim/routines.mjs` mends `wounded` like `minor-injury`. `sim/directors.mjs`: TIMELINE `winter-quarters` … `bexar-end`; `advanceStorming`; `BEXAR_VICTORY`. `public/app.js` shows the grade and marks on the person's card. Tests `tests/storming.test.mjs`; proof `scripts/storming-browser-proof.mjs`.

**Casualties at the record's rates** (owner's correction, 2026-09-16, `docs/COLONIES.md` §7a–c). `rollFates(world, ids, { event, death, wound })` in `sim/army.mjs` is the one rule for a battle's killed and wounded: one seeded roll per person (`seed:id:event`), killed below `1 − (1 − death)^frailty`, wounded below that plus the same for `wound`, with no limit per fight or per class. The exponent keeps small rates at rate × frailty and a rate of 1 at 1, so a battle nobody survived (the Alamo) kills everybody whatever their strength. The rates are each battle's documented shares, rounded: Concepción 0.01/0.02, the storming 0.02/0.08; a dangerous wound's later death is its own roll (`laterDeath` 0.15) and `dieOfWounds` kills every one rolled. The Grass Fight rolls only wounds (nobody was killed). Nothing in `sim/`, `server/` or `public/` assumes one death.

**The towns drawn** (2026-09-16, `docs/TOWNS.md` §9). `sim/town-layouts.mjs` (no imports; served as `/town-layouts.js` by `server/app.mjs`): `TOWN_LAYOUTS` per settlement id with `bearing`, `anchor`, `streets` (`faint`), `squares`, `buildings` (`sprite`, `height` in feet, `label`, `trade`, `filler`); `townPoint(layout, point)` turns frame feet into miles from the site point; `DRAWN_HEIGHT` 4; `dwellings` places ordinary houses on lot centres; `alongRoad` and `scatter` for towns that are a street or a scatter; a layout may carry `walls` (`points`, `sprite`, `spacing`, `height`, `breach`/`breachEvery`), squares with `points`, `riverClearance`, and buildings marked `onWater`. All fourteen towns on the map but Gonzales, Béxar and Lynchburg. `public/town-art.js` `drawTownGround` and `townDrawables`, called from `drawWorld` at scale ≥ 200 like Gonzales; `window.__townsDrawn` for proofs. `sim/shops.mjs` `placesInLayout` gives each keeper a drawn building; the new-town shop sprites are drawn only for a shop with no `building`. Tests `tests/towns.test.mjs`; proof `scripts/towns-browser-proof.mjs`.

**The shops** (2026-09-16, `docs/TOWNS.md`). `sim/shops.mjs`: `TRADES` (each offer `sell`/`buy`/`service` with `refuse` and `give`), `TOWN_TRADES`, `KEEPERS`, `createShopkeepers` (called at the end of `createTownspeople`; Gonzales's blacksmith is `town-pike` given the `blacksmith` deal), `advanceShopkeepers`, `tradesAt`, `counterOptions`/`counterRefusal`/`takeCounter`, and the hooks `gearExertionShare` (in `progressTravel`), `wagonSpeedShare` (in `beginTravel`, and the harnessed beasts take the person's speed), `campRestShare` (in `restAndTire`), `rifleTrue`/`spendRifleShot` (at the shot), `shopsInvalid`. `sim/chores.mjs`: chore `visit-shop` (town, `which-shop` ask, per-trade `trade` and `shop-counter` steps, a `shop` step taking the chosen `trade:offer:pay` flag); asks now receive `(entity, world, household)`; `askAvailability` defers `shop-counter` to `counterRefusal`; a connected shot adds a hide. Keeper ids are `town-<trade>-<settlement>`. The fresh-student payload bound in `tests/chores.test.mjs` moved from 7500 to 8000 for furniture and the shops.

**Furniture** (2026-09-16, `docs/SETTLING_IN.md` §6.1). `sim/furniture.mjs`: `FURNITURE`, `PIECES`, `makeRefusal`, `buyRefusal`, `wanting`, `furnitureShares(household, housed)` (read by `advanceRoutine` for food eaten, spoilage and rest at home), `mindingBaby` (read by the `work` step pacing in `advanceChore`, `BABY_BURDEN` 1.25 on heavy home work), `furnish`, `furnitureInvalid`. `sim/chores.mjs`: chores `make-furniture` (ask first, then `when`-gated timber trip, work and `furnish` step) and `buy-furniture` (town, `trade: 'furniture'`, `carpenter-counter`); `askAvailability` now accepts a `why` function. `sim/town.mjs`: `town-carpenter` in `RESIDENTS` and `CARPENTERS` per settlement, dealing in `furniture`. Hidden from `choresFor` while arriving or fully furnished (payload budget).

**Inside the house** (2026-09-16, `docs/SETTLING_IN.md` §7.2). `sim/interior-data.mjs` (no imports; served as `/interior-data.js`): `INTERIORS` (sprite and `[id, label, x, y]` spots as fractions of the picture), `INTERIOR_OF`, `INTERIOR_ART` (`[sprite, height share, name]`). `sim/interior.mjs`: `interiorOf` (from `landView`; null while camping), `interiorItems`, `placeRefusal`, `placeItem` (action `place-item`), `interiorProjection` (`land.interior` for the family, `overview.lands[].interior` for the Host), `interiorInvalid`. `public/interior.js` `renderInterior`; `public/app.js` records `housesDrawn` per frame, `houseAt` on a tap, `renderInteriorPanel`. Tests `tests/interior.test.mjs`; proof `scripts/interior-browser-proof.mjs`.

**How a family looks** (2026-09-16, `docs/SETTLING_IN.md` §7.1). `sim/appearance.mjs`: `SKIN`, `HAIR`, `CLOTHING`, `HEAD`; `appearanceOf(world, entity)` (a parent's `entity.appearance` over seeded defaults; a child derived from the parents on every call); `looksWords`; `choicesFor`; `appearanceRefusal`/`setAppearance` for the action `set-appearance` (a lobby action, handled before the who-can-act rules); `appearanceInvalid` in `validateWorld`. `familyProjection` adds `appearance`, `looks`, and for a parent `choices`/`chosen`. Nothing else in `sim/` imports it — `tests/appearance.test.mjs` proves inertness behaviourally. Client: `public/appearance.js` (`renderLooks`, `bindLooks`) into `#family-looks`, served at `/appearance.js`.

**The ending** (2026-09-16, [docs/MONEY_AND_GLORY.md](docs/MONEY_AND_GLORY.md) steps 4–5, §7.1). `sim/ending.mjs`: `finalNumber(money, glory)` = `countedCoin(money)` × (1 + glory), where `countedCoin` = max(`COIN_FLOOR` = 1, money) (owner, 2026-09-16); `familyEnding` also returns `counted`; `familyEnding(world, householdId)` → `{ name, money, glory, final, sum, story, coin, awards }`; `hostEnding(world)` → `{ families (household order), winners, best, discussion }`, with automatic households (`automatic` from `sim/neighbours.mjs`) counted and never among `winners`; `endingProjection(world, householdId, role)` returns `{}` unless `world.status === 'ended'`, and is spread into `projectWorld`. Coin lines come from events carrying a signed `coin` field, written by `sell` and money `consume` steps in `sim/chores.mjs` and by both sides of a trade in `sim/trade.mjs`. `dateOf(world, minute)` is exported from `sim/directors.mjs`. Client: `public/ending.js` (`renderEnding`, `bindEnding`) into `#ending` and `#ending-open`, served at `/ending.js`; it computes nothing.

**Coin, and a store counter** (2026-09-12, [docs/MONEY_AND_GLORY.md](docs/MONEY_AND_GLORY.md) steps 1–2). `resources.money` is whole reales (`validateWorld` refuses a fraction; absent reads as none), in `GOODS` and named `real`/`reales` by `goodName`. Buying and selling at the store is a chore `ask` — `cotton-counter`, `powder-counter`, `seed-counter` in `ASKS` — whose options carry `requires` tests that `askAvailability` applies, and whose steps branch with `when`. A counter's `fallback` may be a list: nobody answering pays the first way the family can. `sell` steps with `per`/`gives` pay coin only for whole bundles and leave the rest at home. `needsAny` lets a chore start with any one of several prices, and `COIN` holds every price (`FIC-GONZ-022`). The storekeeper holds a limited `purse` (`STORE_PURSE_PER_FAMILY` in `sim/town.mjs`): the trade step records `traderId`, a coin `sell` pays no more than `purseOf` allows, a coin `consume` refills it, and `purseHeld` reads it without writing so a projection never changes the world. Glory and the ending are specified there and not built.

**And the remembered story stopped riding whole on every tick.** A household's event log was projected in full, for the length of a class: the only genuinely unbounded thing on that channel. It is capped at twice what the journal shows. The honest fix is a delta rather than a ceiling, and [§2 of the reference architectures](docs/REFERENCE_ARCHITECTURES.md) already holds BrowserQuest's incremental sync for when visibility broadens — this is the second reason to want it.

**One skill can be trained, and the narrowness is the design.** `practise-shooting` in [sim/chores.mjs](sim/chores.mjs) raises a person's hunting hand by one for an afternoon and two powder, capped at three. Farming and hands stay exactly as they were dealt at founding, because a household without the handy member having to walk into Gonzales is the pressure that makes the town exist — and hunting has never had anything to do with that pressure.

[docs/REFERENCE_ARCHITECTURES.md §8](docs/REFERENCE_ARCHITECTURES.md) refused Total War's veterancy the day before this was built, and the entry is **amended rather than replaced**: veterancy is a free reward for repetition, this is a decision that spends the very resource the skill is for. Three locks hold the ceiling — the control refuses it, the step clamps it, `validateWorld` refuses a world holding a skill outside one to three — and the middle one is marked `ceiling:` because nothing can reach it today.

**Kin is the world's, names are the student's, and ids are neither.** [sim/family.mjs](sim/family.mjs) holds both halves and keeps them apart. An entity's id never changes — `hh-1-thomas` stays `hh-1-thomas` however often somebody is renamed — which is what `CLAUDE.md` means by stable person IDs, and it matters because `skillsFor` and `visualVariant` are both derived from the id. A rename must not change what somebody is good at or what their face looks like, and it does not. The cost is that an id keeps its founding name, so a student's Bartolo may be `hh-3-thomas` underneath; that is ids not lying about identity, and it is the right trade.

Default names are **dealt** across a class rather than hashed per household. The obvious version — hash the id into a pool and take the remainder — was measurably bad: fifteen households produced seven distinct mothers and a run of four consecutive fathers called Feliciano. A remainder of a hash is not a shuffle. The pools are shuffled once from the world's seed and dealt in order, so fifteen households get fifteen different fathers, and thirty get each name twice, which is what places are like.

Who a family is is **fetched**, not sent. `/api/family` answers for the household on the cookie and nothing else; it is about four hundred and fifty bytes and changes only when somebody renames somebody. That is the same lesson as `/api/map` and `/api/chores`, learned here for the third time. See [docs/evidence/family.json](docs/evidence/family.json).

**Powder is the one resource two different systems spend.** A shot in `sim/chores.mjs` takes one; going upriver in `sim/directors.mjs` takes up to two out of the same house. It is deliberately the smallest possible new thing — one number on `household.resources`, one chore that buys it, one entry in `GOODS` — because what earns its place is not the resource but the tie: the powder a family spends hunting is the powder that is not there when somebody is asked to go.

It touches no outcome it must not. `HIST-GONZ-004` fixes the battle and nothing here moves it; a household with none may still go and carry supplies, which is what the request actually asks. What changes is the family afterwards.

A class saved before any of this has no powder on its households, and the empty value that keeps what was true of them is the **full** house rather than none — those families could hunt. It is filled in `readSave`, the one door every save comes through, so one absent number cannot mean three to a chore and nothing to a trade. No save version moved. See [docs/REFERENCE_ARCHITECTURES.md §8](docs/REFERENCE_ARCHITECTURES.md).

**Every decision is one control.** A call from a neighbour and a question from inside a chore are the same thing to a student, so they are now the same thing on screen: a line of text, and answers that each carry their own price and their own reason for being shut. `requestOptions` in [sim/directors.mjs](sim/directors.mjs) builds a call's answers in exactly the shape `ASKS` gives a chore's, and one renderer draws both. The four hard-coded decision buttons are gone.

`callAvailability` is asked by the control **and** by the handler, which is the half that matters: what a button says is open is what the world will allow. That was a live defect rather than a hypothesis — "Help · 2 food" sat there enabled for a household with one food and failed on the press. `choreAvailability` learned this first; this is the same lesson arriving at the decision the lesson turns on.

And silence is recorded. An unanswered call used to flip to `expired` with no event at all, so a family that never came to the door had no story about it in the log [VISION.md](VISION.md) §20 builds the epilogue from. See [docs/evidence/one-decision-shape.json](docs/evidence/one-decision-shape.json).

**Work can stop and ask.** A chore step `{ ask: 'shot' }` suspends the chore, writes the question and its options onto `entity.chore.ask` — texts and all, computed on the server from the person standing there — and advances nothing until `answer-chore` arrives or the person's own patience runs out (`ASK_PATIENCE`, two fictional hours, the same shape as a rider's). Later steps carry `when: ['take', 'wait']` and run only for the answers they belong to; `settleAsk` records whether the family chose or nobody was listening, because those are two different stories about the same household and the epilogue is built out of them.

The outcome contains no die. `steadyHand` is the whole rule — not tired, and hunting skill of two or more — and both halves are already visible to the student before they answer. It is also where the systems meet: fatigue comes from the road, the road's cost comes from the travel mode, so **how a family travelled decides whether it can shoot straight**. Nothing in that chain is new; it is four things that already existed finally touching.

It is deliberately general. The interpreter knows nothing about hunting: any chore can ask, and the ford, a trade or a crossing are the obvious next ones.

**A hunt is stages the server runs, not an animation the client invents.** It used to be one step — five ticks of standing on the spot the road left them with a searching pose playing. It is now `stalk` steps that move a person about the place they are standing (the same relative move `walk` makes about a yard, and refused outright for anybody on a road, which is the teleport `walk` fell into once), separated by real work steps that each name what is being done. The renderer chooses a pose from those words and draws `musket-smoke` on the one tick the shot is showing. **Nothing about the quarry exists anywhere** — `HIST-GONZ-013` documents buffalo as the only local game and this project names no other, so the kill is smoke and then a carry cycle on the road home.

There is no civilian firing pose in the art library. Dressing a farmer in `volunteer-aim` to borrow one would put a soldier in the timber, so the pose does not change at the shot and the smoke carries it; the browser proof asserts no militia sheet was bound.

**A homestead has state, and all of it can be taken away.** [sim/improvements.mjs](sim/improvements.mjs) holds three things a family has on its land — the cabin, the broken ground, the fence — and every one of them has a state including `ruined`. The ruin path is written, tested and **called by nothing**: `HIST-GONZ-019` anchors it to the Runaway Scrape, and no destruction of homesteads around Gonzales in October 1835 is documented, so none is invented. One test plays a whole class through and asserts that nothing took anybody's property, because a director that quietly started doing so would be a historical claim nobody reviewed.

The field is the interesting one for the renderer. `/api/map` is fetched once a class and never changes, so a field that grows cannot grow in the terrain: the polygon on the map is the whole labor of ground a household holds, and the client draws the share of it the household has actually broken. `window.__fieldRect` was added on the usual seam contract so a proof can measure that it really did grow.

No save version moved: a class saved before this has no `improvements` and no `field.cleared`, and both absences are the state every family used to be in.

**News has no interface of its own.** There was a headline bar and a rider card stacked in the corner of the map; both are gone. The arrival is the rider drawn at the gate plus a mark over the person he stopped, the way in is a **Listen** button on that person's own panel, and the record is the first page of the family journal. Nothing here is new simulation — the rider, the mark and the transcript all already existed — and that was the problem: the cards were a second telling of something the world was already showing.

Two presentation seams were added on the usual contract (read by proofs, by nothing in the application): `window.__viewMarks` says who was marked and why, and `window.__conversation` says how much of an exchange is on screen against how much the family has been told. Both exist because "is the invitation actually in the world" and "does it arrive a line at a time" cannot be answered from a projection.

The conversation is paced client-side and **every word in it is the server's** — this decides only *when* a line appears. It is not a forced reading timer in the sense [docs/LIVING_INFORMATION.md](docs/LIVING_INFORMATION.md)'s attention gate forbids: nothing is removed, nothing scrolls away, the journal holds every line regardless, and `prefers-reduced-motion` puts the whole exchange up at once. A conversation paces once and is never replayed. See [docs/evidence/news-arrives-as-a-person.json](docs/evidence/news-arrives-as-a-person.json).

**A journey has a mode, and the mode owns the trade-off.** [sim/travel.mjs](sim/travel.mjs) is a table of three and imports nothing, because both [sim/world.mjs](sim/world.mjs) (which owns journeys) and [sim/chores.mjs](sim/chores.mjs) (which owns work) have to agree about what a mode is, and `chores.mjs` is handed `beginTravel` as a parameter precisely to keep an import arrow between them from existing.

Property that comes along gets its own travel record on the same route at the same speed, and `borrowedBy` names who took it. Both of those had been in the world since it was first built and neither had ever been used: `borrowedBy` was always null, and the art library has carried `ox-walk`, `horse-walk` and `wagon-loaded-travel` since the atlases landed, so a family's animals had never once been seen to leave the yard. What makes the ox rivalrous is not a lock but a fact - it is somewhere, and if it is not where you are, you cannot take it.

Two guards are worth knowing about. `handleMarch` and `handleChoice` write a decision down and *then* send somebody walking, so both ask `travelRefusal` before anything moves - a journey refused halfway through would leave a household that had agreed to go with nobody on the road. `beginChore` does the same for the same reason, keyed on whether the chore travels at all rather than on whether it hauls: a chore keeps its mode for the journey **home**, and that leg runs inside `advanceChores`, which is inside `stepWorld`. An impossible journey discovered there does not refuse one student's order; it stops the class.

No save version moved. A class saved before any of this has no `mode` on its travel records and no horse in its yards, and both absences read correctly: everybody walked, and that family has no horse. See [docs/evidence/travel-modes.json](docs/evidence/travel-modes.json).

**How fast a class is watched is not how fast the world is.** `PACES` in [server/app.mjs](server/app.mjs) names three tick intervals and the Host may change them mid-class; nothing under `sim/` can see the pace, so a tick is twenty fictional minutes and a settler walks three miles an hour at every one of them. The pace lives outside the world and outside the save, because it describes a room rather than a simulation. `tickMs` rides in the snapshot so the renderer can spread one tick's movement across one tick of real time — without that the client assumes a second, and a slower class walks for a second and then stands still.

This is what the owner's "moving way too fast" actually was. The speeds were right; one real second a tick was not. At 1200× real time a walking figure crossed 8.7 of its own drawn body lengths a second against 0.78 for a real walker, with a correct 2.8-step gait animation playing underneath — life-speed legs and a nineteen-hundred-foot stride. Zoom could never have fixed it: screen speed is scale times miles per second and the drawn height of a person is also proportional to scale, so the scale cancels. See [docs/evidence/pace.json](docs/evidence/pace.json).

**The lobby accepts orders and runs no clock.** `LOBBY_ACTIONS` in [sim/world.mjs](sim/world.mjs) names what a household may do before the class begins — its own work, its own people, its own travel — and `applyAction` refuses everything else with a reason. Anything addressed to another household waits, because that household may not have joined. Two independent guards keep the clock still, the server's tick timer and `stepWorld` itself, so a single mistake in either does not start time early; a test has to break both to observe the difference, and one does.

**Five joined households is a guard, not a wall.** Starting below five is refused once, with a count and an instruction, and a second deliberate press goes ahead. The world always holds `playerCount` households and the ones nobody joined simply take no orders, so the minimum is a pedagogical check rather than a technical requirement — and the way through is what makes a single-machine trial possible without a developer setting no teacher would find.

**Observation is a server rule, not a rendering one.** `observedBy()` returns the people a household may see: anyone standing at a site where one of that household's own people is standing, reduced to who they are, where they are, what they appear to be doing and their visible condition. Skills, chores, travel plans, commitments and a courier's `report` never travel. They arrive as `others`, deliberately a separate list from `entities`, so a person who is not yours cannot be commanded by accident. `tests/town.test.mjs` covers each of those, and the leak assertions were checked by injecting a careless spread and watching them fail.

**Two things are fetched once per class rather than per tick**: the map (`GET /api/map`) and the catalogue of work (`GET /api/chores`). Both are static for the life of a class. This is a pattern worth naming, because it has now been got wrong twice: shaded relief was three quarters of a student's payload before it moved, and the chore table was 4.46 KB of a 6.8 KB payload — six fixed sentences retransmitted to every student every tick. Availability stays on the tick, because it is a permission; the words describing the work do not.

Static art is served by a separate `/assets/` route rather than the fixed file map. It accepts only `png`, `webp` and `json` under `public/assets/`, rejects dot segments, encoded separators, Windows paths, null bytes and hidden files, resolves symbolic links before checking containment, and caps file size. It sends an ETag with `must-revalidate`, so a device downloads the 5.2 MB of sheets once and thereafter revalidates for nothing. `tests/asset-http.test.mjs` exercises the traversal and escape cases.

The Content-Security-Policy is `default-src 'self'` with `style-src 'self'` and `script-src 'self'`. **Inline `<style>` blocks and inline `<script>` are therefore blocked**, which is why every page here links a stylesheet instead of carrying one. Setting `element.style.left` from script is unaffected and is how the selection card is positioned.

`resolveDataDir()` picks the class data folder once, in this order: `TEXAS_DATA_DIR`; `<application folder>/data` whenever it is writable; otherwise `%LOCALAPPDATA%\TexasRevolution\data`. A read-only install directory therefore cannot prevent a class from starting. `resolveSavePath()` puts `classroom.json` in that folder unless `SAVE_PATH` overrides the save file alone. `scripts/appinfo.mjs` prints the resolved answer as JSON so the PowerShell launcher, stop helper and preflight read one source of truth instead of re-deriving the rule. `joinCandidates()` ranks physical adapters ahead of VPN and virtual ones; enumeration identifies candidates and never establishes that another device can reach them.

The lower-level `createClassroom()` factory defaults to `createWorld` and a 200 ms interval. This intentionally creates a scenario-free core for focused tests. Do not mistake that fixture default for the launched game's configuration. Omitting `savePath` creates an ephemeral in-process instance.

## Authority, protocol and identity

One Node process owns one World State. Browsers submit intentions and render returned projections; they never advance authoritative time or publish replacement world state. Simulation functions mutate a supplied plain-data world deterministically. Cryptographic session credentials are separate from the reproducible simulation seed. Seeded randomness currently initializes food; before adding random live outcomes, persist/version the generator state or use stable event-keyed draws so reload cannot change the result.

| Route | Contract |
| --- | --- |
| `GET /`, `/host`, `/app.js`, `/style.css` | Locally served application assets |
| `GET /health` | Listener/application/PID/launcher-nonce identification, plus whether this process is stopping and whether it supports a graceful stop; not a simulation or save-health test |
| `POST /api/host` | Validate private Host key and set Host cookie |
| `POST /api/join` | Validate class code and lobby capacity, issue student identity; existing authenticated students recover their assignment |
| `GET /api/state` | Current authenticated projection |
| `GET /api/events` | Full authenticated snapshots over SSE |
| `POST /api/command` | Validate authenticated role, command ID, action availability and entity ownership |
| `POST /api/rejoin` | Move an existing household to the device holding its family key. No class code, allowed after Start, never creates a household |
| `GET /api/families` | Host only. The roll of joined households by the name each student chose. Carries no keys, so it is safe to leave on screen |
| `GET /api/family-key` | Host only. **One** household's family key, asked for by name, for a student who has lost both their browser and their key |
| `GET /api/map` | Public geography, fetched once per class |
| `GET /api/chores` | The catalogue of work, fetched once per class |

Host actions are `start`, `pause`, `resume`, `end`, `new-class` and `stop-server`; every other action reaches `applyAction` as a student intention and is rejected there. `stop-server` exists only when the process supplied an `onStopRequested` callback — `server/main.mjs` does, in-process test fixtures do not, and the Host projection carries a `canStop` flag so the control is never offered by a build that cannot honour it. It checkpoints a running class as **paused** before responding, because a saved running class resumes advancing on restart; then a transient `lifecycle` overlay is broadcast so connected browsers learn the teacher stopped the server before their streams end, and the process closes after a short flush delay.

`new-class` is refused unless the class is `lobby` or `ended`, so a live class cannot be discarded. It archives the last completed checkpoint through `archiveSave()`, then rotates `sessionId` and `sessionCode`, clears client assignments and command ledgers, and rebuilds the world from a fresh seed while keeping the configured class size. Because cookie names embed the session ID, rotation invalidates every previous student credential by construction; the response also re-issues the Host cookie under the new name and expires the old one, and student streams are closed so a previous student cannot silently inherit a household in the new class. Cookie names are therefore read per request rather than captured once.

Student bearer credentials are random tokens stored in `HttpOnly; SameSite=Strict` cookies with a seven-day maximum age. The save stores their SHA-256 hashes mapped to household assignments and display names. Identical display names are allowed; a name is never a recovery credential. The Host uses a distinct random key, kept in the private save and initial Host URL fragment. The browser exchanges that fragment for its HttpOnly cookie and removes the fragment from the displayed address. Neither key nor the student credential registry belongs in snapshots.

**Trading between households lives in [sim/trade.mjs](sim/trade.mjs) and is deliberately physical.** An offer exists only between two people standing at the same site; `together()` is the single predicate that decides it, and `advanceOffers()` runs each tick after travel and chores and deletes any offer whose two people have parted. There is no market, no order book and no way to deal with a household across the county, because a global market is the optimisation spreadsheet `CLAUDE_DEVELOPMENT_ROADMAP.md` §1D forbids. `world.offers` is keyed by id; the actions are `offer`, `accept-offer`, `decline-offer` and `withdraw-offer`, and all four are open to **any** family member rather than the principal, which is the whole point of the feature.

The ownership guard is the one `applyAction` already had. Every trade action names one of *our* people — the person making the offer, or the person it was made to — so "choose one of your family" is also what stops one household answering for another; on top of that, `respondToOffer` checks the offer's own household and the named entity. Goods are `food` and `seed` only, in whole amounts of one to twenty, and an offer must swap one good for a different one. Nothing is escrowed: affordability is re-checked at acceptance, and an offer whose goods were spent in the meantime fails with a plain sentence and closes. That is marked in the source with a `ceiling:` comment naming reservation as the upgrade path.

**No save version moved.** A class saved before trading existed simply had no offers, which is a correct empty answer rather than a missing field, so `world.offers` defaults on load and `validateWorld` accepts its absence. The alternative — bumping `saveVersion` to 4 — would have refused to open every existing class in exchange for nothing.

**What crosses the wire is one side of a bargain.** `offersFor()` writes every offer from the reading household's own point of view (`weGive` / `weGet`), carries the other person's name, their family's fictional name and their entity id, and carries nothing about their household's stores. The other person is necessarily already visible in `others`, because an offer cannot outlive standing together. `observedBy()` now also reports an observed person's family name, since every household is a copy of the same four names and `Thomas` alone cannot distinguish two of them; that name is the world's own fiction and is never the display name a student typed to join, which lives beside their credential on the server and is not part of the world at all.

**A family key is how a household gets back in.** Every household has one, eight symbols of a Crockford base32 alphabet — no I, L, O or U, so it can be read off one screen and typed on another without the 0/O and 1/I confusions. It is **derived, never stored**: `HMAC-SHA256(hostKey, sessionId + ':' + householdId)`, five bits per symbol, so no new saved state exists to migrate, no key survives `new-class` (which rotates `sessionId`), and forty bits make it unguessable at classroom scale. `POST /api/rejoin` takes only that key: it needs no class code, it works **after Start**, and it never creates a household — it moves an existing one to the device holding the key, minting a fresh credential and deleting the old one so a household keeps exactly one command ledger. Three rules bound it. A household whose stream is open **now** is refused with 409, because a key read off a neighbour's screen must not evict them. Wrong keys are throttled per address, five tries then a thirty-second cooldown. And a household is told its own key and no other — **the ordinary Host projection carries none of them**, because a teacher's screen is sometimes a projector. A student who has lost both their browser and their key is recovered through `GET /api/family-key`, which returns exactly one household's key on request; the Host control that uses it is closed by default, shows one family, and clears itself after thirty seconds. Revealing a key is not recorded anywhere, which is marked in the source with a `ceiling:` comment. `tests/rejoin.test.mjs` covers each, and the live browser proof is [docs/evidence/family-key-recovery.json](docs/evidence/family-key-recovery.json).

**Presence separates *away* from *gone*.** A mobile browser drops its SSE stream within seconds of the screen locking, so a count of open streams told a teacher that a student had left when they had not. The Host projection now carries `presence: { here, away, joined }`: `here` is streaming right now, `away` is a household whose stream closed within a ninety-second grace window. It is held in memory beside `runtimeFault` and `lifecycle` and is **deliberately never saved** — it describes this moment, not the class. Students are told nothing of it. The older `connected` field is unchanged and still means streams open now.

New joins require the lobby, correct code and an available slot. The supported configuration range is 5–30; Start requires five assigned households. Live connected count is separately computed from unique households with SSE streams. Reconnection with an existing credential works after Start and does not allocate another family. Each credential has its own authority: body fields cannot grant a different role or household.

Every accepted command has an ID retained in a per-student or Host ledger of **256 IDs**. Repeating a retained ID returns `duplicate: true` without another commit. This is bounded replay protection, not an indefinite exactly-once guarantee. The ledger is persisted. The UI has no durable offline action queue; failed actions surface an error.

SSE sends full projected snapshots tagged with the committed `revision`. Native `EventSource` reconnects; the server sends current state rather than replaying a `Last-Event-ID` history. Do not treat revisions as an event archive. Connection-count broadcasts may repeat a revision, and the emergency fault overlay can change a snapshot without a new committed revision. Each household is limited to three streams; a receiver buffering more than 1 MiB is disconnected rather than growing an unbounded queue.

JSON POSTs have a small body limit and an Origin check when an Origin header is present. Static content uses CSP and a no-referrer policy. These checks do not turn plaintext HTTP into an Internet service. See the deployment boundary below.

## Persistent world and two renderings

Session cookies include the persistent session ID in their names. Cookies are scoped to host/path rather than port; the namespace prevents distinct classes on one computer from overwriting one another's credentials.

`world.entities` is the canonical ID-to-entity registry. Every important person, ox, wagon and courier has one identity and one location. Households reference member/principal/property IDs and hold food, relationships, commitments and remembered event IDs. Supporting references must survive absence; they are not instructions to relocate an object.

A stationary location has world coordinates and a valid `siteId`. A traveler has `siteId: null`, endpoints, its own resolved polyline, distance/progress/speed, purpose and causal departure ID. Movement interpolates along that polyline and records departure, first travel progress and arrival. Arrival changes the existing entity; it creates no destination copy. Return travel resolves a fresh path in the other direction.

**Drawing movement** (display only, `public/motion.js`, 2026-09-16). `ProjectionMotion` draws a traveller linearly from where they were drawn when a tick arrived to the progress the server sent, across the snapshot's `tickMs`. A snapshot of the same tick and minute with a new revision (a command somewhere in the class) keeps the current walk; a next tick whose minute step is in `CALENDAR_STEPS` (the values of `CALENDAR_SCALE`) is a walk, anything else is snapped as a time jump. `GaitClock` plays a traveller's walk, ride or rolling clip by the ground drawn under them in their own drawn height (`STRIDE`, wheels by `clipGait`), capped at the authored rate and floored at `GAIT_FLOOR`; `animated()` in `public/app.js` takes it as `gait`. Nothing here reaches `sim/`: `tests/movement.test.mjs` pins the pace constants and a walk's ground, and `npm run test:movement` measures painted frames.

**One world unit is one mile.** A person walks `WALK_SPEED` of one mile per twenty-minute tick; a courier rides at `RIDER_SPEED`. `sim/geography.mjs` builds the region from the checked claims in [HISTORY.md](HISTORY.md): the Guadalupe as terrain, Gonzales on its east bank, the contested ford opposite the town just above the forks of the Guadalupe and San Marcos, Williams's land about seven river miles above the ford on the west bank, two stands of timber, and homesteads scattered along the road and water at varied distances rather than on a grid. Every homestead is on the east bank and no track or bank road crosses a river: only the `crossing` route does, once (`tests/geography.test.mjs`). Every coordinate is invented under `FIC-GONZ-002`; the pattern is anchored.

Sites carry a `kind` — `town`, `homestead`, `ford`, `camp`, `woods` or `junction`. A junction is a shape in the road network and not a place: it has no building and no name on the map. `world.map.terrain` holds `{ id, kind, points }` features for the river, timber, fields and the town commons.

Travel resolves a route through the graph with `findPath()`, a Dijkstra over `world.map.routes`. That is what makes the river a barrier: a household on the east bank reaches the west bank only by way of the ford, because no edge crosses the water elsewhere. Distances therefore differ sharply between households — in a fifteen-household class the nearest home is under two road miles from town and the farthest nearly nineteen.

The student has one map. A single canvas consumes the permitted `snapshot.world.entities` and draws each at its authoritative location under one continuous camera; figure size and label density derive from the camera's scale rather than a local/regional mode. The camera follows the household and widens to include the route while anyone travels; the player may drag or zoom, or choose one of their own people from the roster to watch, and the camera then keeps up with that person as they move until Follow is pressed. A household spread across the county cannot be framed legibly all at once, which is why picking a person to look at exists at all. Moving the camera never moves a person or broadens authorization. The wider public picture is the Host's projected screen, not a second student panel. Cosmetic offsets separate overlapping drawings without changing truth. [docs/ASSETS.md](docs/ASSETS.md) defines feet anchors, coordinates, placeholder proportions, future sprite sizes, animation names and level of detail.

The current template has four named people per household. Thomas has detailed depth; supporting family members and couriers have moderate depth; animals/property use aggregate labels. These are architecture categories, not three completed AI systems. Battle formations hold aggregate position/phase/count data. Canvas draws representative little soldiers from those records, capped at 24 per formation. Decorative soldiers are not independently simulated entities, exact historical troop totals, or additional versions of household characters.

## Truth, reports and fog of war

`world.truth` stores objective topic records. `world.knowledge.households[householdId]` and `world.knowledge.public` separately store received reports. Reports support rumor, unconfirmed, confirmed and contradicted states; received time and observed-event time have separate derived ages. **Only a firmer account is news**: `wouldLearn` refuses the same report twice and refuses a fainter version of something a household already has better, and it is the one predicate used both by a rider deciding whether to stop and by the journal deciding whether to write something down.

**A family is rolled** (`FIC-GONZ-021`, 2026-09-12, [docs/FAMILY_CREATION.md](docs/FAMILY_CREATION.md)). `roll-family` is a lobby action: `familyRoll(seed, householdId)` gives one to twenty (`FAMILY_DIE`; six before 2026-09-14), `compositionFor(roll, die)` reads `FAMILY_FACES` (or the six-sided rule for a household with no `die`), `rollFamily` stores `household.die = 20`, and validation checks the members against the face, `rolledPeople` makes the parents and children with `sex`, `age`, kin and hidden `traits` (`strength`, `health`, `housework`), and `rollFamily` replaces the founding four with ids `hh-N-parent-1` … `hh-N-child-4`, principal first. `rollRefusal` allows it only while the family's record holds nothing but its founding; the server's Start rolls any joined family that still can. Traits never reach a client: the entity projection is a whitelist, `observedBy` is a whitelist, `familyProjection` never reads them, and `tests/family-roll.test.mjs` searches every payload for planted values. `tooYoung` (under ten) refuses work, travel and calls in `choreAvailability`, `modeAvailability`, `callAvailability` and `applyAction`. Absent `sex`/`age`/`traits`/`roll` read as the founding household, so **no save version moved**.

**Stand-in art** (2026-09-12). A student's own people are projected with `sex`, `age` and `band` (`ageBand` in `sim/family.mjs`: infant, small, child, youth, adult); `observedBy` sends only `sex` and `band`. `castVariant` in `public/motion.js` picks the figure from those instead of an id hash, keeping `rust` for the local principal, and `figureScale` shrinks children in `miniPerson`. Both are marked `stand-in:` and listed in `docs/ART_REQUESTS.md`; somebody with no stated sex keeps the id-hash figure.

**Who took part, and where they stood** (2026-09-12). `formationMembers` in `sim/directors.mjs` is everybody whose march was accepted and who stands at `CAMP_SITE`; `standWithTheForce` places each a few yards behind the Texian formation in an id-ordered slot every tick from the gathering on, leaving `siteId` at the camp so witnessing and observation are unchanged. `settleHelp` writes `world.participation.gonzales[personId] = { householdId, role: 'supplied' | 'present', minute }`, which nothing projects. `beginTravel` prepends the person's own position when they stand more than `STANDING_APART_MILES` (0.25) from the place's point. `world.participation` absent reads as none, so **no save version moved**.

**Every report in the slice is carried in person** (2026-09-12). `CONVERSATIONS['gonzales-outcome']` in `sim/encounters.mjs` makes the outcome a rider topic, and its truth is sited at `CAMP_SITE`, so `dispatchReport` starts riders at the camp and `advanceRelays` hands it on. The night crossing is a truth (`CROSSING`, `HIST-GONZ-003`) established at `TIMELINE.crossing` and learned only by `witnessing` households; `offerMarch` requires it and words the call by `receivedMinute - observedMinute`. `sendRider` skips a rider name already in the report's provenance.

**What the hidden stats do, and who answers** (steps 3 and 4, 2026-09-12). `housekeepingSaving` in `sim/family.mjs` reduces what `advanceRoutine` counts as eaten by the best `housework` among people at home; `heavyWorkPace` multiplies a `heavy: true` chore's work ticks by strength. Both are 1 for anybody with no `traits`, so the founding family's numbers do not move. `canAnswerCalls` (a parent, or a stated age of 16+) gates `callAvailability`; `applyAction` no longer requires the principal for `help`, `stay`, `go-see`, `stay-home`, `go-upriver` or `stay-in-town`. `handleChoice` and `handleRumor` record `actorId` on the answer, `offerMarch` and `settleHelp` read it, and `directorProjection` sends `request.answerers` — options per eligible person — with `options` kept as the principal's for older clients.

**What a family knows decides what it is asked** (`FIC-GONZ-020`, 2026-09-12). At the notice the director puts a rider on the road to every household at once, so road distance alone decides when each hears; there is no longer a household-order schedule or a family told on the spot. `offerRequests` reads the household's report: `firmEnough` (confirmed or unconfirmed) opens `world.requests[id]`, the neighbour's call, with `where: 'home' | 'town'`; a rumor opens `world.rumors[id]`, a question projected as `request.kind: 'rumor'` with `go-see` and `stay-home`. Going is an ordinary walk; `witnessing` makes the word confirmed in Gonzales and the call opens there. Firmer word marks an open rumor `overtaken`; a refused rumor suppresses the later call. `world.rumors` absent reads as none, so **no save version moved**. `advanceEncounters` runs two passes so that when two riders reach one family in the same tick, the one sent to that family speaks. `stepWorld` (and `resolveTimeJump`) also calls `advanceEncounters` once before `advanceRelays`, and a rider that finishes a leg keeps it as `report.lastLeg` until that call has looked along it: somebody on the stretch ridden in the tick the rider reached a fork is met, the rider put back halted beside them, before the word changes hands (2026-09-14). `lastLeg` never outlives the tick, so no save version moved.

**A courier is a physical world entity, and there are now two ways one discharges an errand.** A topic with no authored conversation still delivers on arrival at the destination site. A topic with one - `cannon-request`, and only that - is delivered by being **said to a living person who is actually there**, and is carried in legs: a rider goes about `LEG_MILES` and hands the word to somebody going further, who hands it on again. Each hand costs one step of confidence, so the same event reaches a near household as a confirmed report and a far one as a rumor, and `report.provenance` carries the source, the observation minute and everybody who has carried it. The hand-off itself costs no simulated time, so road distance alone still decides *when* a household hears.

`projectWorld()` selects the permitted top-level fields, own household/entities, own events and own reports; the Host receives public events/reports, and in place of `observedBy` the whole class as `others` plus `overview` (`sim/overview.mjs`). Event cause IDs are filtered to events visible to that viewer. Travel projections omit private purpose/cause internals. Raw truth, other household knowledge, seed, future timeline, barriers, complete director state and session credentials are excluded. Director projection separately admits local battle state or public reconstruction frames only when the viewer is eligible. Adding fields to household or report records requires a projection review: those permitted records currently travel with their existing fields, so private future metadata must not be attached casually.

The current observation model is deliberately narrow: students see their own physical household, battle formations when a family member is at Gonzales, anybody standing where one of their own people is standing, and - the one exception to standing-next-to-somebody - **a rider carrying news within `SIGHT_MILES` of one of their own people**, because news that materialises at the moment it is spoken has no approach at all. Other families exist in the shared world but are not otherwise visible to students. **The Host has no fog** (owner, 2026-09-16): `sim/overview.mjs` sends it every person, animal and wagon at its true position and every family's land as it stands, whitelisted field by field; see *The teacher sees the whole class* above. The schematic map's sites/routes are public geography. Broader local visibility needs a server-side visibility model and regression tests; never send hidden entities first and rely on canvas filtering.

## Time, history and Host autonomy

One live `stepWorld()` advances one tick and 20 fictional minutes, progresses travel, applies routines, delivers reports, then advances directors. Timer delays do not trigger a wall-clock catch-up burst. With the launched 1000 ms interval, the 5680-minute slice takes approximately **284 ticks / 4 minutes 44 seconds**, excluding pauses and processing delay. The scenario projection derives `historicalDate` from a fixed September 29, 1835 UTC epoch plus elapsed world minutes; the UI displays that date. The approach falls on October 2. Within-day timings and travel duration are gameplay abstractions. The whole game's roughly 45-minute lesson remains future scope.

The Gonzales factory registers each historical milestone as a compression barrier. Directors establish truth, dispatch news, offer one request per household, close unanswered requests, advance gathering/approach/exchange/withdrawal/resolution, settle personal consequences, release public outcome news and end the slice. Milestone flags, request outcomes, pending couriers, battle state and reconstruction samples are saved, so reconnect/restart does not replay initialization.

The macro-outcome is fixed independently of player choices: Mexican withdrawal and cannon retention by the Texian side. The claim registry and fiction boundaries are in [HISTORY.md](HISTORY.md). Nearby households can see the live battle. The Host remains on public regional information until outcome news becomes public; then it automatically focuses Gonzales and shows labeled delayed reconstruction frames. These are stored phase samples, not a full replay engine. End-of-slice preservation is not the final game's revelation, debrief or epilogue.

`resolveTimeJump(world, minutes)` is an in-process primitive, not a teacher control. It accepts whole-minute jumps up to 60 days in a running world. It stops at the earliest unresolved registered barrier or immediately before significant principal help/service arrival. The caller resumes live stepping to show that moment. Blocked retries are no-ops.

Compression advances elapsed minutes in at-most-20-minute segments using the same travel, food, delivery and minor-recovery routines. It does not add live ticks or execute director milestones inside the jump. Routine visits may complete; significant arrivals wait for a visible step. Existing relationships, loans, service, absences, deaths and major conditions remain. Routine logic creates no death/capture/severe injury, does not settle loans/service, and records a compression event plus household routine summaries. Any new important event system must register barriers; the resolver cannot infer future narrative importance automatically.

## Checkpoints, faults and recovery

The save envelope uses `saveVersion: 3`; the world uses `schemaVersion: 3`. Both went to 2 when real geography replaced the schematic grid, and to 3 when farm work added tools and a field to every household — in each case because a class built on the older model would open into a world missing something it needs. Validation checks entity IDs/locations/references, resources, tools, field state, offers, event references and knowledge consistency. These guards are a foundation, not a complete migration or hostile-file validation system. Unknown save versions fail instead of silently starting a new class.

**A version is bumped when an old save would open a world that is wrong, and not otherwise.** Family keys, presence and trading between households all shipped without moving it, because each has a correct empty answer for a class that predates it: a derived key needs no stored field, presence is never saved, and a class with no offers had none. Bumping would have refused to open every existing class in exchange for nothing. Growth that *does* require a bump is growth where the empty value would be a lie.

Before loading or creating a persistent session, the server acquires an exclusive `<save>.lock` against a canonical local path. The lease is independent of port and released only by its matching PID/token on clean shutdown. Stale or ambiguous locks fail closed. This prevents two servers from forking and overwriting one class history.

A mutation clones the prior session, applies the change, validates, increments revision, writes a temporary checkpoint, flushes the file and renames it over the main save before broadcasting success. On write failure the mutation rolls back and a transient unsaved fault pauses the live class. Resume retries a checkpoint before clearing the fault. The last completed file may still say running, so an emergency in-memory pause is not a durable Pause.

A graceful stop is now available from the Host page and from `Stop.vbs`, and `server/main.mjs` removes the launcher's process record on its way out when that record describes itself. `New Class` writes an `archive/` copy of the finished class before replacing it. Full operational instructions and limitations are in [docs/RECOVERY.md](docs/RECOVERY.md). There is still no automatic migration, rotating backup of an ongoing class, damaged-save wizard, identity-transfer wizard, archive-restore UI, settings screen, or always-visible running/stopped indicator while the server is hidden. Closing a browser or choosing End Game does not stop the server. Directory flushing after rename, sudden power loss and cross-machine filesystem leases are not covered guarantees.

## Extension and verification seams

Use `createWorld(seed, count)` or `createGonzalesWorld(seed, count)`, explicit `applyAction()` inputs, `stepWorld()`, `resolveTimeJump()`, snapshots and the event graph for deterministic headless scenarios. `createClassroom({ worldFactory, ... })` supports fixture injection only by trusted in-process code. There is no HTTP phase selection, state mutation, truth inspection or time-skip debug route. `window.__snapshot`, `__received`, `__viewEntities` and `__viewFormations` expose already-delivered presentation evidence, not omniscient state.

`runScenario({ seed, playerCount, strategy, maxTicks })` runs the same Gonzales world without rendering, accepts `mixed`, `help`, `stay` or `idle` bot strategies, and returns world, recorded inputs and metrics. For example, `npm run simulate -- repeatable-seed 15 mixed` prints metrics. The CLI defaults to five households; it does not change the launched game's 15-slot default. Headless bots can begin a running world directly because this is private developer code, not a student-facing solo mode or a bypass in the HTTP Start route.

`extractMetrics(world)` reports per-household survival/health, food, available wagons, separation, lost property, decisions, time since a decision, repeated requests, persistent consequences and memories. These are internal observations, not a score. Evacuation is explicitly `not-modeled`; inherited consequences are `null`. The current decision metric counts recorded choices and is not a completed measure of story quality or parity.

The persistent Event Log carries ID, type, simulated time, actor/household, causal IDs, importance, visibility and optional historical claim/classification. It supplies future deterministic epilogues and current internal metrics. The UI shows only the most recent 12 own events; the saved log remains complete. It is not yet an event-sourced replay system. Checkpoints, whole-state cloning, full snapshots and unbounded event history need profiling before longer arcs; current transport observations are not long-session performance guarantees.

Regression files cover networking, 30-client HTTP/SSE capacity, entity continuity, knowledge/time, persistence failures and the evolving Gonzales slice. Run `npm test`; use [HANDOFF.md](HANDOFF.md) for the current browser commands and evidence. Protect ownership, one-world identity, wire isolation, continuous story, historical anchors and valid refusal before adding content.

## Deployment boundary and unfinished foundation work

[docs/DEPLOYMENT.md](docs/DEPLOYMENT.md) separates same-machine browser evidence, independent-device LAN checks and district validation. **Physical-device LAN and actual district validation remain NOT YET TESTED.** HTTP bearer credentials require a trusted network; do not expose this prototype publicly. SSE's per-browser HTTP/1 connection limit also means many tabs in one profile are not a classroom load test.

The launcher bundles a checksum-verified Node executable and keeps an installed-Node fallback; `scripts/bundle-runtime.ps1` produces the runtime, its license and a manifest, and `scripts/launch.ps1` refuses a bundled runtime that does not match its recorded checksum. Graceful lifecycle controls and double-click launch now work on the development machine. A signed executable, an approved district package, and an always-visible server status surface remain operational work. Historical geography/travel calibration, researched representative households, broader shared local visibility, pacing/balance, accessible classroom usability and save migrations remain partial. Later Texas Revolution arcs are not implemented. Extend these seams without creating a second world or making the teacher operate historical triggers.
